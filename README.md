# www.thaiannebutze.com.br

Public landing page for thaiannebutze.com.br, deployed via GitHub Pages.

## Deploy

Push to `main` — GitHub Actions deploys automatically.

Custom domain: `www.thaiannebutze.com.br` (see `CNAME`).

## Regenerate

Content and design are generated from the company repo (`thaiannebutze.com.br/`) by `/startup.site`.
